# MultiTimer
Multi-timer system for performing sedimentation-based granulometric analyses in batch mode 

Each button starts a set of 3 synchronized countdowns with predefined durations.
Useful for running multiple analyses in parallel (batch mode) while keeping precise timing logs.
